doctype html
html(lang='nl')
    head
        meta(charset='utf-8')
        title Article
    body
        h1 #{firstName}
        h1 #{lastName}
        br
        form(action = '', method = '')
            button(type="submit") Edit
        br
        form(action = '', method = '')
            button(type="submit") Delete